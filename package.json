{
  "name": "phonebook_backend",
  "version": "0.0.1",
  "description": "",
  "license": "MIT",
  "author": "Johnny",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd /Users/johnny/Documents/fullStackOpen/exercises/phonebook && npm run build && cp -r dist /Users/johnny/Documents/programs/phonebook_backend",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "morgan": "^1.11.0"
  }
}
